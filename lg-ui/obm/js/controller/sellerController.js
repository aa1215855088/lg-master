//控制层app.controller('SellerAuditController' ,function($scope,$controller,TbSellerService){    $controller('baseController',{$scope:$scope});//继承    //读取列表数据绑定到表单中    $scope.findAll=function(){        TbSellerService.listSeller().success(            function(response){                $scope.list=response.result;            }        );    }    $scope.findOne=function(id){        TbSellerService.findOne(id).success(            function(response){                $scope.entity= response.result;            }        );    }   // 分页    $scope.listPage=function(page,rows){        TbSellerService.listPage(page,rows).success(            function(response){                $scope.list=response.result.rows;                $scope.paginationConf.totalItems=response.result.total;//更新总记录数            }        );    }    //$scope.searchEntity={};//定义搜索对象    //搜索    $scope.search=function(page,rows){        TbSellerService.findPage(page,rows,$scope.searchEntity).success(            function(response){                $scope.list=response.result.rows;                $scope.paginationConf.totalItems=response.result.total;//更新总记录数            }        );    }      /*  //保存        $scope.update=function(){            var serviceObject;//服务层对象            if($scope.entity.id!=null){//如果有ID                serviceObject=TbSellerService.update($scope.entity); //修改            }            serviceObject.success(                function(response){                    if(response.success){                        $scope.reloadList();//重新加载                    }else{                        layer.msg(response.message);                    }                }            );        }*/    $scope.status=['待审核','已审核','未通过','已关闭'];    $scope.update = function(sellerId,status){        TbSellerService.update(sellerId,status).success(function(response){            if(response.code=="200"){                //重新查询                window.location=location;//重新加载            }else{                layer.msg(response.message);            }        });    }});